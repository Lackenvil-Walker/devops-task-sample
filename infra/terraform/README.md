# Terraform (Docker provider)

This Terraform module creates the external Docker network + volumes that `docker-compose.yml` expects.

## Usage (on the target server)

```bash
cd infra/terraform
terraform init
terraform apply
```
